# Changelog

## [Unreleased] — ACode rebranding wave

### Planned
- Brand sweep: replace user-facing `kilo*` references with `acode` across README, UI strings, telemetry events, settings labels
- New provider: `nvidia` (40+ models via `integrate.api.nvidia.com/v1/chat/completions`)
- Remove upstream GitHub references (`KiloOrg/*`, `SFARPak/kilocode`) from CI, install scripts, telemetry
- Marker rename: `kilocode_change` → `acode_change`, allowlist script updated
