{
	"name": "@acode/build",
	"description": "ESBuild utilities for ACode.",
	"private": true,
	"type": "module",
	"main": "./dist/index.js",
	"types": "./src/index.ts",
	"scripts": {
		"lint": "eslint src --ext=ts --max-warnings=0",
		"check-types": "tsc --noEmit",
		"test": "vitest run",
		"build": "tsc",
		"clean": "rimraf dist .turbo"
	},
	"dependencies": {
		"zod": "^4.1.8"
	},
	"devDependencies": {
		"@acode/config-eslint": "workspace:^",
		"@acode/config-typescript": "workspace:^",
		"@types/node": "20.x",
		"vitest": "^3.2.3"
	}
}
